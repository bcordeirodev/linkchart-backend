services:
  app:
    image: linkchartapi-test
    container_name: linkchartapi-test
    ports:
      - "8081:80"
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
      - APP_KEY=base64:REDACTED_TEST_APP_KEY
      - JWT_SECRET=REDACTED_TEST_JWT_SECRET
      - DB_CONNECTION=sqlite
      - DB_DATABASE=/tmp/test.db
      - CACHE_STORE=file
      - SESSION_DRIVER=file
      - QUEUE_CONNECTION=sync
      - LOG_CHANNEL=stack
      - LOG_LEVEL=info
    volumes:
      - ./storage/logs:/var/www/storage/logs
    networks:
      - test-net

networks:
  test-net:
    driver: bridge
