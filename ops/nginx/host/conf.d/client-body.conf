# Uploads (bio avatar = 2MB no app) + folga p/ multipart e API futura.
# Default do nginx era 1M — cortava avatares validos de 1-2MB com 413 na borda
# (bug do upload de avatar em 2026-08-02). Aplicado em /etc/nginx/conf.d/.
client_max_body_size 10m;
